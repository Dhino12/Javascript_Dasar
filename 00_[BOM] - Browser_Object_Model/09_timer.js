// set time
let time = window.setTimeout(alert("Hello"), 3000)

// remove time
window.clearTimeout(time)

// setInterval
let interval = window.setInterval(alert("Hai"), 1000)

// stopInterval
clearInterval(interval)